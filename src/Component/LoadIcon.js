import React from "react";
import image from "../assets/refresh-svgrepo-com (2).svg";

const LoadIcon = () => {
  return (
    <div className="load">
      <img src={image} alt="reload icon page" />
    </div>
  );
};

export default LoadIcon;